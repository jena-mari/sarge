# Algorithm source

Place pure, deterministic allocation and evaluation modules here. Functions should accept explicit inputs, return explicit outputs, and avoid network, database, clock, and global-state dependencies.
